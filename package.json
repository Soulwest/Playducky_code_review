{
	"name": "click-tracker-api",
	"version": "0.0.1",
	"description": "ClickHouse-based click tracker API",
	"type": "module",
	"main": "dist/server.js",
	"scripts": {
		"build": "tsc",
		"start": "npm run build && node dist/server.js",
		"dev": "ts-node server.ts",
		"test": "jest"
	},
	"dependencies": {
		"fastify": "^4.0.0",
		"@clickhouse/client": "^1.0.0",
		"axios": "^1.0.0"
	},
	"devDependencies": {
		"jest": "^29.0.0",
		"ts-jest": "^29.0.0",
		"nodemon": "^2.0.0",
		"typescript": "^5.0.0",
		"ts-node": "^10.0.0"
	},
	"jest": {
		"preset": "ts-jest",
		"testEnvironment": "node"
	}
}
